import React from "react";

//Header component
//Destructures heading propr whcih contains the course.name
export const Header = ({ heading }) => {
  return <div>{heading}</div>;
};

export default Header;
